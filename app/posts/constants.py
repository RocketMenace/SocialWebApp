class ErrorCode:
    NOT_VALID_DATA = (
        "Parameter image_url can only take values 'absolute' and 'relative'"
    )
    POST_NOT_FOUND = "Post not found"
    FORBIDDEN = "Only owner can delete posts."
